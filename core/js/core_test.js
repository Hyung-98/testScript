var core = window[APP_NAME],
    ui = core.ui

ui.TEST = function(){
    var $$ = core.Selector(arguments[0],{})
    
    var helper = this.events = {
        _init: function(){ 
            
        }
    }
}